extend type Query {
  """
  -- add queries here --
  -- check the monograph for a hint --
  """
}

"""
-- will need a couple of types here --
"""


enum ActivityCategory {
  LEISURE
  ADVENTURE
  CULTURE
}
